#include <iostream>
#include <cmath>

using namespace std;

int main()
{
   
   
    int a,b,c,brutto ;
    
    cin >> a >> b ;
    cout << (a*a) + (b*b) ;

   
    cin >> a >> b;
    cout << (a+b)*(a+b);

    
    cin >> a >> b;
    cout << pow(a-b,3);

   
    cin >> a >> b >> c ;
    cout <<( a*b*c);

   
   cin >> a >> b ;
   cout << /* 2*(a+b)/5 << */ (a-b)*0.4 ;

 
   cin >> brutto ;
   cout << brutto/1,23;

   
   cin >> a >> b ;
   cout << a%b ;

}
